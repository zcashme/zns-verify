[package]
name = "zns-verify"
version = "0.1.0"
edition = "2021"
description = "ZcashName (ZNS) hash, action, and memo primitives -- used by Registry minters and Resolvers"
license = "MIT"
repository = "https://github.com/zcashme/zns-verify"
homepage = "https://github.com/zcashme/zns-verify"
keywords = ["zcash", "zns", "orchard", "verification", "names"]
categories = ["cryptography", "cryptography::cryptocurrencies"]

[features]
# Default is the pure verification kernel: no_std, no orchard, pasta/sinsemilla
# math only -- droppable into a wallet, SDK, enclave, or embedded target.
default = []

# `decrypt` adds ZNS Ironwood trial decryption for scanning Name Notes.
# Name Notes are Ironwood-pool notes (NU6.3) whose cmx is derived from the
# name binding, not from rseed; ZnsIronwoodDomain handles that rule. It pulls
# orchard + zcash_protocol and forces std, so it is strictly opt-in --
# anyone who only needs to *verify* a binding never compiles it.
decrypt = [
    "dep:orchard",
    "dep:zcash_protocol",
]

[dependencies]
blake2b_simd = { version = "1", default-features = false }
pasta_curves = { version = "0.5", default-features = false }

# For computing the expected Sinsemilla note commitment from the
# caller-supplied (g_d, pk_d, v, ρ, ψ, rcm) -- entirely from crates.io,
# no orchard fork required.
sinsemilla = "0.1"
group = { version = "0.13", default-features = false }

# ── `decrypt` (ZNS Ironwood trial decrypt, optional) ─────────────────────────
# The orchard fork with unsafe-zns, pinned to its release tag.
orchard = { git = "https://github.com/zcashme/orchard.git", tag = "zns-orchard-v0.15.5.2", default-features = false, features = ["std", "unsafe-zns"], optional = true }
zcash_protocol = { version = "0.10", default-features = false, features = ["std"], optional = true }

[dev-dependencies]
hex = "0.4"
hex-literal = "0.4"
